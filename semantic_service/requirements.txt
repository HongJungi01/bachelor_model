# torch / torchvision are installed separately from the CUDA-specific
# PyTorch index in run_semantic.bat — do NOT list them here, or pip will
# resolve them against default PyPI (CPU wheels) when this file is used.
fastapi
uvicorn[standard]
pillow
numpy
ultralytics>=8.4
opencv-python-headless
pyyaml
